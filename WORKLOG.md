# 제작 이어받기 기록

## 2026-09-22 · 시뮬레이터 학습 흐름 강화

- 전체 57개 실습에 예측 → 실행 → 설명 안내를 공통 적용했다.
- 현재 상태, 이번 변화, 다음 관찰 지점을 단계마다 나누어 보여 준다.
- 이전 상태 복원과 단계 기록 번호를 추가해 앞뒤 상태를 비교할 수 있게 했다.
- 단계형 실습에 느리게·보통·빠르게 자동 실행 속도를 추가했다.
- 실습 모형의 범위와 한계는 접어서 본 화면의 인지 부담을 줄였다.
- 모바일에서는 안내와 결과 설명이 한 열로 자연스럽게 이어지도록 정리했다.
- 단계형 실습은 단계 수, 자유 조작형 실습은 조작 횟수로 구별하고 새 기록이 없는 조작도 현재 결과를 설명하도록 교정했다.

## 목표

사용자 요청: “완성해와”. 확정 작업 계획의 24개 대단원·48개 소단원 전체 HTML을 완성하고 검증한다. 첫 단원만으로 범위를 축소하지 않는다.

추가 요청: “이어서해 이미지 예시도 만들어 가면서 작업해주길 바래”. 단원 설명에 필요한 생성 이미지를 함께 제작하여 HTML에 내장한다. 첫 푸드트럭 이미지를 생성·검토하고 assets/에 복사했다. 사용 프롬프트는 assets/foodtruck-prompt.md에 기록했다.

## 2026-09-16 현재

- 구현: CH01~CH24의 학생용 원고 48개, 문제 144개, 대표·보조 실습 57개. 확정한 24개 대단원·48개 소단원의 학생용 본문 구현을 완료했다.
- 공개 화면: 사이트 제목을 `Computer Science`로 통일하고 기본 진입점에 CS 학습 이유·전체 과정 지도·학습 방법을 설명하는 개요 페이지를 추가했다.
- 참고자료: 1강 CH01~CH06의 학생용 참고자료와 내부 검토 기록에서 기존 Notion 링크를 제거하고 기술 원문 링크만 공개한다.
- 배포 구조: README, `.gitignore`, GitHub Pages Actions 워크플로를 추가했다. `main` push 시 `npm ci` → 전체 테스트 → 단일 HTML 빌드 → `dist/` 배포를 수행한다.
- 코드: classic JavaScript 원본을 scripts/build.cjs가 단일 HTML로 묶는다. 원본 목차는 확정 커리큘럼 MD에서 읽는다. 외부 글꼴·CDN은 없다.
- 산출물: dist/index.html, dist/cs-course.html. 개발 소스 content/, src/labs/, src/app.js, src/style.css.
- 로직 확인: CH01~CH24의 상태 모델 테스트 204개를 작성했다. 카나리 버전별 오류율·롤백, 빈 관측의 데이터 없음 처리, 최근접 순위 p95, RPO·RTO 목표 비교와 모순 시각 거부를 포함한다.
- 실제 브라우저: Codex IAB(Chromium), http://127.0.0.1:4173. CPU 중간·최종 상태, 99+99=198, 빈값·소수 오류, 기존 결과 유지, 저장하지 않은 글의 전원 복원, 오답 해설, LRU 히트2/미스3, RR A9/B5/C3 완료·평균대기8/3 확인.
- 모바일: 390×844에서 가로 넘침 없음, 목차 열기·단원 이동, Enter 한 단계 실행 확인. 전체 크기/확대/모션 감소/file/offline 검증은 아직 남아 있다.
- 직접 열기 제약: Browser Use가 file:// 경로 방문을 보안정책으로 차단했다. 다른 브라우저·우회 경로로 같은 file 접근을 시도하지 않는다. 허용된 localhost 미리보기 검증과 file 직접 열기 검증을 구별하고 후자는 미확인으로 남긴다.
- WebMCP: read_cs_lesson, step_cs_lab이 실제 UI와 같은 상태에 연결되어 기본 읽기·단계 실행 확인. 잘못된 입력 검증은 추가 확인 예정.
- 원본 근거: content/출처_검토기록.md. CH01 두 페이지의 접힌 텍스트를 전체 읽고 핵심 오해를 수정했다.
- SQL: sql.js 1.14.2와 SQLite WebAssembly를 단일 HTML 안에 포함했다. localhost에서 SELECT·GROUP BY·JOIN·문법 오류 후 재실행을 실제 SQLite 3.49.1로 검증했다.
- 이미지 기준: 사용자의 추가 요청에 따라 소단원마다 3~5개의 별도 이해용 이미지·도해를 배치한다. 한 이미지 안에 여러 장면을 합치지 않고 비유·구조·변화 장면을 분리하며, 프롬프트를 assets의 Markdown에 기록한다.
- 이미지 반영: CH01~CH03 여섯 소단원은 생성 이미지 각3장, CH04~CH24은 소단원별 반응형 개념 도해3~4장을 사용한다. 생성 원본 PNG를 보존하고 단일 HTML에는 1280px JPEG를 내장한다.
- CH04 브라우저 확인: 255의 8비트가 11111111, A가의 UTF-8이 41 EA B0 80, 8×8 RGB 원시 데이터가 192B로 계산됐다. 큐에서 A를 먼저 꺼냈고, A와 E가 같은 해시 버킷에 충돌해도 함께 남으며 Set에서는 같은 A가 중복되지 않음을 확인했다.
- CH05 브라우저 확인: 4B 정수10개와 복사본의 입력40B·보조40B·합계80B, 최근20개 모드에서 25개 추가 후 저장20개, 1~16의 16 탐색에서 순차16회·이진5회, 10MB/10Mbps/RTT100ms의 단순 합계8.100초를 확인했다. 정렬되지 않은 이진 탐색 입력은 기존 결과를 보존하고 이유를 표시한다.
- CH06 브라우저 확인: HI 메시지가 9단계를 거쳐 복원되고 목적지 없음은 수신값 없이 중단된다. 2·1·3 조각은 1·2·3으로 재구성되고 2번 누락은 재요청 후 완료된다. 도서 B01은 HTTP200, B99는404, 연결 실패는 상태코드 없음으로 구별된다. TLS 유효 인증서는 보호 통신까지, 만료 인증서는 검증 단계에서 중단된다.
- CH07 브라우저 확인: 3A·1B·2C·1D를 안정 삽입·버블 정렬해 모두 1B·1D·2C·3A가 되고, 잘못된 입력은 마지막 유효 결과를 보존한다. 동전1·3·4와 금액6에서 탐욕3개·DP2개, 동전4·6과 금액5에서 두 방식 모두 도달 불가, 금액0에서 각각0개를 확인했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH08 브라우저 확인: 기본 BST 중위 순회1·2·3·4·6, 키5 추가 뒤1·2·3·4·5·6, 키3 탐색 경로4·2·3을 확인하고 중복 키를 기존 상태 보존과 함께 거부했다. 같은 그래프에서 BFS는 A-C 1개 간선, 다익스트라는 A-B-C 비용2를 찾았고 연결되지 않은 D와 음수 비용을 구별했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH09 브라우저 확인: 파일 읽기 정상 흐름은 KERNEL 검사·WAITING·READY를 거쳐 USER의 저장된 위치로 HELLO와 5 bytes를 반환했다. 권한 없음은 내용 없이 EACCES, 사용자 모드 직접 장치 접근은 PROTECTION으로 끝났다. 두 칸 파이프는 A·B 뒤 C 쓰기 대기, 읽기 뒤 C 전달, A·B·C FIFO와 EOF를 보였고 빈 파이프 대기·읽기 끝 닫힘 뒤 EPIPE도 구별했다. 각 차시 그림4개·문제3개, 390×844 페이지 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH10 브라우저 확인: 가상주소2053을 페이지2·오프셋5로 나눠 TLB MISS와 페이지표의 프레임7을 거쳐 물리주소7173으로 변환했다. 주소1024는 오프셋0·PRESENT=0 폴트이며 잘못된 주소는 기존 결과를 보존했다. 요청1·2·3·1·4에서 FIFO는1, LRU는2를 내보내며 히트1·폴트4였다. 저널 정상 저장은B, 커밋 전 장애 복구는A, 커밋 후 장애 복구는B였고 0바이트 파일과 ENOSPC의 기존A 보존도 확인했다. 각 차시 그림4개·문제3개, 390×844 페이지 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH11 브라우저 확인: 두 작업이 모두100을 읽고 쓰면 최종101의 갱신 손실이 생기고, 뮤텍스로 읽기·계산·쓰기를 보호하면102가 됐다. 다른 작업 소유의 잠금 해제와 자원 탈취는 거부됐고 한 작업자에서는 최종101과 완료1/1이었다. A가X를, B가Y를 보유한 반대 순서는 실제 대기 순환과 완료0을 만들었고, X→Y 순서 통일은 완료2/2였다. 라이브락은 상태 변화4회·완료0, 기아는 A 반복 진행·B 계속 대기로 구별했다. 각 차시 그림4개·문제3개, 390×844 페이지 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH12 브라우저 확인: 정규화한 관계에서 S1 이름 한 곳을 바꿔도 T1·T2의 S1 참조가 유지되고, 중복 한 표의 첫 행만 바꾸면 같은 S1이 두 이름으로 보이는 수정 이상이 생겼다. 대출0건 회원은 독립 저장되고 중복 기본키와 없는 외래키는 거부됐다. 실제 SQLite3.49.1에서 회원별 집계 M1=2·M2=1, 올바른 JOIN3행·조건 빠진 JOIN6행·빈 조회0행·NULL 검사를 확인했다. 문법 오류 뒤 정상 쿼리 재실행도 성공했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH13 브라우저 확인: 리프 최대3키 모형에1~4를 넣어 [1,2]·[3,4] 분할과 루트 구분키3을 만들었다. 키4 검색은 페이지2개, 범위2~4는 연결 리프를 포함한3개를 방문했고 중복 키는 거부됐다. 실제 SQLite3.49.1 계획은 제목 인덱스 전 SCAN books, 생성 뒤 SEARCH books USING INDEX idx_books_title로 바뀌었다. 기본키는 자동 인덱스를 사용하고 함수 적용 조건은 다시 SCAN이었다. 3표 JOIN은 loans 스캔 뒤 회원·도서 기본키 검색을 보였다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH14 브라우저 확인: 문장별 최신 확정 버전 모형에서 T1의 두 읽기는100→120, T1 시작 스냅샷 모형은100→100이었다. T2의 미확정120은 두 규칙 모두 읽지 않았다. WAL 모형은 COMMIT 내구화 전 장애에서100을 유지하고, UPDATE와 COMMIT 내구화 후 데이터 반영 전 장애에서120을 복원했다. 반복 복구도120을 유지하고 변경 없는 거래는100이었다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH15 브라우저 확인: 192.0.2.130은 /25의 R2, 192.0.2.20은 /24의 R1, 다른 목적지는 /0의 R3를 선택했고 기본 경로 제거 시 경로 없음으로 끝났다. 잘못된300 주소는 이전 결과를 보존하며 거부됐다. TCP 모형은 A 뒤 ACK2, B 유실·C 보관 뒤에도 ACK2·앱 A, B 재전송 뒤 ACK4·앱 ABC·재전송1회를 보였다. 정상 흐름은 재전송0회이고 중복 B는 앱에 다시 전달되지 않았다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH16 브라우저 확인: 실제 Promise 반응 B와 0ms 타이머 C를 동기 A 뒤 예약해 A→B→C를 확인했고, 잘못 예상한 A→C→B와 차이를 표시했다. 연속 실행은 매번 새 로그 A→B→C로 끝났으며 준비된 오류는 이전 결과를 보존했다. 문서 메모리에만20px를 저장한 뒤 새로고침 모형은 기본18px로 돌아갔고, sessionStorage·localStorage에20px를 저장한 뒤 실제 페이지 새로고침에서도 두 값이 남았다. 저장 차단 상황은 기존 값을 유지했으며 검증 뒤 교안 전용 키를 삭제했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH17 브라우저 확인: 공개 예문 hello의 UTF-8 Base64는 aGVsbG8=로 변환·복원됐고 Web Crypto SHA-256은 2cf24d…9824의 독립 참조값과 일치했다. hello!는 다른64자리 해시, 안녕은7JWI64WV로 복원됐으며 빈 입력의 표준 해시·101글자·미지원 경계를 구분했다. 권한 모형은 S1의 A1 읽기와 담당 교사 T1의 읽기를200으로 허용하고 S2·다른 수업 T2·로그아웃·없는 대상을403·401·404로 거부했다. 담당 교사의 수정과 숨긴 버튼 우회도403이었고 img 모양 입력은 DOM 요소0개인 텍스트로만 표시됐다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH18 브라우저 확인: 실제 제한 문법 파서가 2+3×4=14, (2+3)×4=20, 숫자 하나7과 중첩 괄호20을 계산했다. 2+는 피연산자 위치, 8/(3-3)은 0 나누기, 2+a는 허용되지 않은 문자 위치를 표시하고 마지막 유효 결과20을 보존했다. 도달 가능성 모형은 처음 A·B 도달/C·D 후보, A→B를 끊으면 B·C·D 후보, 루트를 끊으면 네 객체 모두 후보, C를 루트로 추가하면 모두 도달로 계산했다. 루트 없는 C↔D 순환은 종료됐고 없는 Z 참조는 거부됐다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 콘솔 오류 없음도 확인했다.
- CH19 브라우저 확인: 5단계 파이프라인에서 독립 명령과 ALU 전달 사용은7사이클·멈춤0회, 전달 없음은11사이클·멈춤4회, LOAD 직후 사용은8사이클·멈춤1회였다. 분기 예측 성공은7사이클, 실패는 고정 비용2를 더한9사이클이며 빈 명령 목록은 거부됐다. 암달 계산은 50%·8코어1.78배, 90%·8코어4.71배, 모두 직렬1배, 모두 병렬·8코어8배, 90%·매우 많은 코어10배 한계를 보였다. 95%·16코어 선택은9.14배·무한 코어 상한20배로 갱신됐고 코어0 오류는 직전 상태를 보존했다. 초기 UI 설정 객체 오류를 실제 브라우저에서 발견해 회귀 테스트와 함께 수정했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 수정 후 새 콘솔 오류 없음도 확인했다.
- CH20 브라우저 확인: 비동기 쓰기는 A만 v1인 상태에서 B의 old 읽기와 1버전 지연을 보였고 B에 복제를 전달하면 최신 v1로 바뀌었다. B 분리 중 모두 전달은 C만 갱신했으며 연결 복구와 재전달 뒤 세 복제본이 수렴했다. 동기 예제는 A+B 과반수 저장 뒤 응답했고 없는 Z 읽기는 거부됐다. Raft 모형은 A 로컬 제안은 미확정·효과0회, B 복제 뒤 확정 index1·효과1회, 같은 req-1 재시도는 로그와 효과를 늘리지 않았다. A 분리 뒤 B+C로 term2 리더 B를 선출하고 req-2를 C에 복제해 index2·효과2회를 확정했으며 복구 뒤 세 로그가 일치했다. 한 서버만 저장은 미확정, 새 키는 두 번째 효과, 같은 키·다른 내용은 오류와 직전 상태 보존을 확인했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 새 콘솔 오류 없음도 확인했다.
- CH21 브라우저 확인: 120개 요청과 서버2대×40의 기준은 초과40개였고, 3대 수평 확장과 캐시50%는 초과0개였다. 총 용량이120으로 충분해도 세션 고정은60·40·20으로 초과20개, 핫키80%는96·12·12로 초과56개였으며 핫키 분할 뒤40·40·40과 초과0개가 됐다. 큐는 8개 도착·3개 처리의 무제한 모형에서 5초 뒤25개가 쌓였고, 제한 큐는 대기7·거부18, 백프레셔는 대기7·상류 지연18이었다. 소비자를12개/초로 늘려 대기0까지 회복했고 잘못된 서버0대·큐 한도0은 직전 상태를 보존하며 거부됐다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 새 콘솔 오류 없음도 확인했다.
- CH22 브라우저 확인: 메일을 문자로 바꾸는 요구에서 뒤섞인 구조는 대출·저장·알림 3개 역할, 인터페이스로 분리한 구조는 문자 구현·조립 2개 역할이 영향을 받았고 두 구조 모두 B01 대출 저장과 문자 알림1건 결과를 냈다. 구독자0명은 대출 성공·알림0건 정책을 보였고 계층 위반 의존은 거부됐다. 주문 흐름은 모듈형 모놀리스의 원격 경계0개와 동기 서비스의2개를 구별했으며, 재고 장애는 주문 실패, 알림 필수 실패는 보상 필요, 별도 재시도 정책은 주문 완료·알림 대기로 남았다. 이벤트 소비 지연 뒤 재처리하면 주문 완료·알림 전송으로 수렴했다. 직접 상태 변경 문구 오류를 브라우저에서 발견해 수정·회귀 검사했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 새 콘솔 오류 없음도 확인했다.
- CH23 브라우저 확인: 잘못된 Set 기반 정렬은 [2,1,2]를 [1,2]로 만들어 오름차순만 통과하고 항목·길이 보존은 실패했다. 빈 배열·원소1개에서는 버그 함수도 통과해 사례 다양성의 필요를 보였고, 고정5사례에서 버그 함수2/5·수정 함수5/5 통과를 확인했다. 배송비 모형은 > 비교에서 9,999원·10,001원은 통과하지만 10,000원만 실제3,000원·기대0원으로 실패했고, >= 수정 뒤 세 경계값3/3과 0원 사례가 통과했다. 빈 입력과 음수는 각각 별도 오류로 기존 상태를 보존했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 새 콘솔 오류 없음도 확인했다.
- CH24 브라우저 확인: 10% 카나리 20개 요청은 v1 18개·v2 2개, 전체 오류율10%·v2 오류율100%였고 롤백 뒤 새10개는 모두 v1로 가며 새 오류0개였다. 50% 경로는10개를5·5로 나눴고 관측0개는 오류율0%가 아닌 데이터 없음, 110%는 오류로 처리했다. 지연10ms 9개와1000ms 1개는 평균109ms·최근접 순위 p95 1000ms, 모두10ms는 두 값 모두10ms, 빈 표본은 데이터 없음이었다. 실제 손실 구간10분·복구15분을 RPO·RTO와 독립 비교했고 모순된 백업 시각을 거부했다. 각 차시 그림4개·문제3개, 390×844 가로 넘침 없음, 의도한 입력 오류를 초기화한 뒤 새 콘솔 오류 없음도 확인했다.
- 미리보기 서버: scripts/serve.cjs, 포트 4173. 중복 서버를 시작하지 말고 기존 세션이 살아 있는지 먼저 확인한다.

## 2026-09-21 — 시험·복습·이미지 배포 기능

- `src/assessment/`에 범위 출제·선택지 섞기·채점·시간 만료·응시 상태 모델과 localStorage 저장 계층을 추가했다.
- 48개 소단원에 6개씩 총288개 시험 문항을 연결하고 기존 확인 문제의 오답·해설 열람을 복습 노트로 연결했다.
- 시험 목록·단원 선택·준비·응시·결과·재도전 화면과 모바일 스타일을 추가했다.
- 웹판은 참조 JPEG 18개를 `dist/assets/`로 분리하고 단일 파일판은 이미지 내장을 유지했다. 웹 HTML은 약5.5MB에서 약2.4MB가 됐다.
- `npm run build`, `npm test` 217개 통과. Chromium에서 `#exams`와 `#exam/setup/stage-1` 준비 화면을 확인했다.
- 완료: GitHub Pages Actions run `35599349729`의 build/deploy 성공을 확인했고, 공개 HTML 200·분리 JPEG 200·`assets-manifest.json` 200을 확인했다.

## 2026-09-21 — 시험·복습 2차 보강

- 시험 문제은행을 기존 확인 문제와 자동 학습 목표 조합에서 분리했다. `content/exam-bank.js`에 48개 소단원별 6개, 총 288개의 상황·예측·적용형 문항을 새로 작성하고 각 문항을 본문의 서로 다른 6개 주제에 연결했다.
- 응시 저장소에 15초 탭 소유권 임대와 heartbeat, 명시적 이어받기, pagehide 소유권 반환, 저장소 차단 시 현재 탭 메모리 보존을 추가했다. 새로고침 뒤 같은 문항·답변·남은 시간을 복원한다.
- 복습 노트에 복습 중·완료·전체 상태 필터와 확인 문제·시험 출처 필터를 추가했다. 완료한 항목도 다시 확인할 수 있고, 관련 단원의 기존 `summary` 세 항목과 본문 왕복 링크를 제공한다.
- 기존 단원 확인 문제를 복습용 영구 ID로 변환해 오답과 해설 열람이 다시 정상 기록되도록 연결했다.
- `npm test` 220개와 `npm run build`를 통과했다. 실제 Chromium에서 답안 새로고침 복원, 두 번째 탭 보호·이어받기, 확인 문제 복습 기록, 핵심 세 가지, 본문 왕복, 완료 필터를 확인했다.
- GitHub Pages Actions run `35607770361`의 build·deploy가 성공했다. 공개 HTML 200 응답과 신규 문항 문장, 완료 필터, 탭 이어받기 코드 반영을 확인했다.

## 다음 작업

1. 전체48개 소단원의 이전·다음·해시 이동과 목차 상태를 연속 검증한다.
2. 360/768/1280/1440 반응형, 확대·모션 감소, 초기화·단원 이탈 타이머와 WebMCP 오류 입력을 전체 연결 검증한다.
3. localhost 검증과 구별해 file 직접 열기·오프라인 제약 및 최종 실행 안내를 점검한다.
4. 발견한 회귀만 수정한 뒤 단일 HTML의 최종 크기·무결성·실행 절차를 기록한다.

## 2026-09-22 — 실습별 학습 안내·시험 문항 정밀 교정

- 57개 실습마다 서로 다른 예측 질문, 관찰 지점, 설명 과제와 흔한 오해 안내를 작성했다. 단계형·버튼형 실습 모두 실행 상태에 맞춰 다음 안내가 바뀐다.
- 실습 ID와 안내 ID가 정확히 일치하고 네 안내 문장이 모두 채워졌는지 회귀 테스트로 고정했다.
- 48개 소단원 × 6개, 총 288개 시험 문항을 구조·중복·선택지·해설·본문 연결 기준으로 다시 점검했다.
- 기본키의 안정성, 전위 순회 해설, 요구 페이징 조건, 경로 MTU 대응, sessionStorage 범위, SHA-256 비교, 전자서명 신뢰 조건, 과반수 교집합, p95 예시의 모호하거나 부정확한 표현 9곳을 교정했다.
- 문제은행 revision을 3으로 올리고 정규화한 문제·선택지 중복, 최소 문장 길이, 해설 완결, 태그와 복습 연결을 자동 검사하도록 보강했다.

## 2026-09-22 — 후속 학습 품질 고도화 계획과 대표 미션 구현

- 사용자 요청에 따라 직전 제안의 1·2·3·5번을 계획으로 정리하고, 4번 모바일·키보드 중심 전 과정 점검은 제외했다.
- `docs/학습품질_고도화_작업계획.md`에 원고·그림·실습 대조 검수, 시험 선택지별 해설, 비교 미션, 종합 사례의 범위·데이터 구조·실행 순서·완료 기준을 작성했다.
- `docs/학습품질_검수대장.md`에 48개 소단원과 6개 진행 묶음을 등록했다. 이번 재검수의 완료 수는 모두 0으로 시작한다.
- `docs/비교미션_설계목록.md`에 실습 57개별 A/B 조건·관찰 지표를 작성하고, 대표 6개를 상세 설계했다. 현재 UI에 없는 이벤트 루프 준비 코드 등은 보완 필요로 명시했다.
- 문서의 참조 경로와 목록 수량을 검사하고 BST 경로·서버 분산·큐 적체·암달·p95의 주요 설계값을 기존 모델과 대조했다.
- `content/exam-feedback.js`와 시험 생성 로직을 추가해 대표 실습 6개 단원 36문항에 정답·오답 선택지별 해설과 revision 4를 연결했다. 기존 252문항은 기존 revision 3과 공통 해설 경로를 유지한다.
- `src/learning/`에 비교 미션 상태 모델·localStorage 저장·실습 상태 어댑터를 추가하고, CPU 덧셈·BST·그래프·TCP·SQLite 계획·이벤트 루프 6개에 A/B 실행 기록·확인 항목·20자 관찰 문장 조건을 연결했다. 결과가 실제 기대값과 맞을 때만 기록되도록 했다.
- Goodput 표기를 `유효 처리량 (Goodput)`으로 고치고 query-plan 안내를 실제 SQLite detail 출력에 맞췄으며, 단원 시험 안내를 실제 6문항·10분 흐름과 맞췄다.
- 전체 테스트 226개와 빌드를 통과했다. 이번 변경은 아직 GitHub Pages에 배포하지 않았다.

## 2026-09-22 — R1(CH01~04) 학습 품질 고도화 완료

- CH01~04의 8개 소단원에서 본문 48주제, 확인 문제 24개, 그림 25개, 실습 13개를 서로 대조했다. 각 소단원은 6개 주제·3개 확인 문제·3~4개 도해를 유지한다.
- R1의 남은 시험 42문항에 선택지별 오답 해설을 작성했다. 기존 대표 단원과 합쳐 revision 4 문항은 78개이며, 전체 288문항 중 210문항이 다음 작업으로 남는다.
- R1의 13개 실습에 조건 A/B 준비, 실제 상태 판정, 결과 기록, 확인 항목과 설명 제출을 연결했다. 기존 대표 미션을 합쳐 18/57개가 구현됐다.
- 시뮬레이터 초기화와 비교 기록 초기화를 분리해 A를 기록한 뒤 실습을 재설정해도 기록이 유지되게 했다. `기록 지우기`를 눌렀을 때만 비교 기록을 비운다.
- CPU 덧셈은 계산 직후와 RAM 기록 직후, 그래프는 BFS의 1간선·비용5와 다익스트라의 2간선·비용2를 실제 상태로 판정한다.
- SQLite 실행 계획 예시는 같은 SELECT 결과도 함께 보여 주며, 이벤트 루프는 Promise 우선 예약과 타이머 우선 예약을 각각 실제로 실행해 두 경우 모두 A→B→C인지 비교한다.
- 미션 정의 revision이 바뀌면 이전 기록을 재사용하지 않도록 저장 상태 호환 검사를 추가했다.
- 전체 테스트 230개를 통과했고, Chromium에서 R1 소단원의 긴 화면과 비교 미션 배치·버튼·기록 영역을 확인했다.

## 2026-09-23 — R2(CH05~08) 학습 품질 고도화 완료

- CH05~08의 8개 소단원에서 본문 48주제, 확인 문제 24개, 그림 31개, 실습 12개를 서로 대조했다.
- CH05~07의 36문항에 선택지별 오답 해설을 추가했다. 기존 CH08과 완료 단원을 합쳐 revision 4 문항은 114/288개다.
- 공간·제한 버퍼·탐색·네트워크 지연·계층 메시지·조각 순서·HTTP·TLS·안정 정렬·동전 전략 10개 비교 미션을 추가했다. 기존 CH08 미션을 포함해 R2는 12/12, 전체는 28/57개다.
- 공간 미션의 설계값을 실제 모형에 맞춰 합계 변수 4B와 전체 복사본 40B 비교로 교정했다.
- 순차/이진 탐색 16회/5회, RTT 100/200ms의 합계 8.100/8.200초, HTTP 500/연결 실패, TLS 4단계 완료/2단계 중단, 탐욕 3개/DP 2개를 실제 상태 어댑터로 확인했다.
- R2 관련 모델·미션·시험 집중 검사 50개를 통과했다.

## 2026-09-23 — R3(CH09~12) 학습 품질 고도화 완료

- CH09~12의 8개 소단원에서 본문 48주제, 확인 문제 24개, 그림 32개, 실습 8개를 서로 대조했다.
- 48문항에 선택지별 오답 해설을 추가해 revision 4 문항이 162/288개가 됐다.
- 시스템 콜·파이프·가상 메모리·저널 복구·카운터 경쟁·교착·관계 설계·SQL JOIN의 8개 비교 미션을 추가해 전체 36/57개를 구현했다.
- 권한 허용/거부, 파이프 WAITING/READY, FIFO 퇴출1/LRU 퇴출2, 커밋 전 A/후 B 복구, 보호 없음101/뮤텍스102, 교착 완료0/순서 통일 완료2를 실제 상태로 판정한다.
- 정규화된 S1 참조 2건 유지와 중복 한 표의 이름 불일치, 올바른 JOIN 3행과 조건이 빠진 JOIN 6행도 결과 상태로 확인한다.
- R3 관련 모델·미션·시험 집중 검사 53개를 통과했다.

## 제작 규칙

- 실습 정의는 validate/initial/step/describe/render 또는 custom action 구조를 사용한다.
- UI에서 입력 문자열과 적용한 파라미터를 구별한다. 입력 변경은 초기화하고, 잘못된 입력은 마지막 유효 상태를 보존한다.
- current lesson만 DOM에 렌더링한다. 페이지 이동 시 타이머·리스너를 정리한다.
- 참고 자료는 검증한 1차 자료와 사용자 원본을 사용한다. 원고에서 제품별 보장·모형의 한계를 명시한다.
- 브라우저 자동화의 빈 문자열 fill이 IAB에서 적용되지 않은 사례가 있으므로 빈 입력 테스트는 Ctrl+A, Backspace 후 DOM value를 확인한다.

## 2026-09-23 — R4~R6 및 종합 사례 완료

- CH13~24의 24개 소단원에서 본문 144개 주제와 확인 문제 72개를 실습·요약과 대조했다. R1~R6 전체 집계는 본문 288개 주제, 확인 문제 144개다.
- 남은 시험 126문항에 선택지별 해설을 추가했다. 전체 288문항이 revision 4를 사용하며 정답 해설 288개와 선택지별 오답 해설 576개를 제공한다.
- B+ 트리·격리 수준·WAL 복구·IPv4 라우팅부터 카나리 배포·관측과 복구까지 남은 21개 비교 미션을 추가했다. 모든 57개 실습이 실제 상태 어댑터로 A/B 결과를 검증한다.
- 느린 웹사이트, 데이터 복구, 서버 확장 종합 사례를 추가했다. 각 사례는 관찰→가설→실험→비교→설명의 5단계, 독립 도해 3개, 실제 실습 증거 기록, 관련 본문 왕복과 브라우저 임시 저장을 지원한다.
- 종합 사례는 자동 점수 대신 학생의 설명과 자기 점검을 기록한다. 직접 주소로 들어가거나 증거가 부족해도 오류 대신 다음 행동을 안내한다.
- 종합 사례 모델·경로·저장·도해·실습 연결 회귀 검사를 추가했다. Chromium에서 사례 목록, D-01의 5단계 이동, 관측 실습 증거 기록, 관련 본문 왕복을 확인했다.

## 2026-09-23 — 종합 사례 학습 흐름 검수

- 직접 주소로 마지막 설명 단계에 들어가 설명문·자기 점검만 채워도 완료로 표시되는 문제를 재현했다. 완료 조건에 처음 가설·선택 이유, 서로 다른 실습 두 개의 A/B 결과, 가설 판단, 개선안, 최종 설명과 자기 점검을 연결했다.
- 사례 증거를 실습별 A/B로 따로 보존하고 판단 화면에서 각 결과와 관찰 문장을 함께 보여 준다. 제한 큐와 백프레셔의 요약은 거부와 상류 지연을 구별하도록 수정했다.
- 완료 뒤 설명을 고치면 재확인이 필요하도록 하고, 손상된 초안이나 저장소 차단으로 이전 완료 상태가 잘못 복원되지 않게 했다. 저장소 차단 시 현재 탭의 임시 기록과 새로고침 제한을 안내한다.
- Chromium에서 두 실습의 A/B 결과 4개 기록, 가설·판단·개선안 입력 뒤 완료, 완료 후 수정 시 해제, 390px 화면 가로 넘침 없음과 콘솔 오류 없음을 확인했다.

## 2026-09-23 — 학습 개요와 첫 단원 도입 보강

- CS의 뜻을 초심자 언어로 설명하고, 검색 결과가 늦게 나타나는 상황을 브라우저·네트워크·서버·데이터베이스·화면의 다섯 단계로 풀었다. 단계마다 원인을 좁혀 볼 질문을 넣고, 모든 클릭이 서버와 데이터베이스를 거치는 것은 아니라는 모형의 범위를 밝혔다.
- 1강부터 부록까지의 학습 지도에 각 단계의 핵심 질문을 넣었다. 학습 방법은 예상→한 조건 변경→이유 설명으로 구체화하고, 1단원 도입을 3 + 2의 계산·기록·출력 차이와 연결했다.
- 데스크톱과 390px·320px 모바일에서 개요, 학습 지도 이동, 첫 단원 진입, 가로 넘침과 콘솔 오류를 확인했다. 자동 검사 240개와 빌드가 통과했다.
